language: julia
os:
  - linux
  - osx
julia:
  - 1.0
  - nightly
notifications:
  email: false
after_success:
  - julia -e using Pkg; cd(Pkg.dir("PackedMatrixMultiVectorProducts")); Pkg.add("Coverage"); using Coverage; Coveralls.submit(Coveralls.process_folder())';
